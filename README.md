# My CV

A tool for generating and updating my CV. Designed to automate the headache of
keeping a CV up to date.

## Usage

```bash
uv run cv sync
uv run cv build-static
uv run cv build-frontend
uv run cv build-api
uv run cv serve-api
uv run cv serve
```
